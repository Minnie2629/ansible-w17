---
# tasks file for apache
- name: install apache
  yum:
    name: " {{ apache_pkg }}"
    state: " {{ pkg_state }}"

- name: start daemon
  service:
    name: "{{ service_name }}"
    state: "{{ service_state }}"

-name: 